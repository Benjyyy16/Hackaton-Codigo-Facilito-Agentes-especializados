# Vacío a propósito: el frontend pega a rutas relativas y Vercel proxea /live,
# /auth, /providers y /projects hacia el backend (ver rewrites en vercel.json).
#
# Así el navegador solo habla con el dominio del propio sitio y CORS no entra en
# juego. El backend en producción debe permitir el origen de FRONTEND_URL:
# https://hackatondatgent.vercel.app.
#
# Si algún día se quiere volver a llamar al backend directo, hay que poner acá su
# URL y agregar el dominio de Vercel a FRONTEND_URL en el backend.
VITE_BACKEND_URL=
