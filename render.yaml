services:
  - type: web
    name: commitment-twin-backend
    runtime: python
    pythonVersion: 3.12
    buildCommand: bash build.sh
    startCommand: bash start.sh
    autoDeploy: false
    envVars:
      - key: PYTHON_VERSION
        value: 3.12
      - key: PORT
        value: 8000
